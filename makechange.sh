git status  
#lists new or modified files not yet committed

git diff #{file...}
#list down changes and conflicts ok>?

git reset #{file.....}
#unstages file,keeping the file changes

git commit -m "[descriptive message]"
#commit all the stages to virsioned history
